🎯 Task 26.10 (ที่ควรทำต่อ): Simplify Product Documents Modal

เป้าหมาย:
ให้ Modal นี้เป็นแค่ “Product Images / Assets” ไม่พูดถึง Pattern อีกแล้ว

สิ่งที่ควรทำ:
	1.	ซ่อน / ลบทุกร่องรอยของ Pattern ใน UI
	•	เอา table “แพทเทิร์น” ออกจาก modal
	•	ปรับเป็นปุ่มรูปภาพแทน
	•	เอาคอลัมน์หรือปุ่มที่เกี่ยวกับ pattern ออกจาก JS
	2.	Backend
	•	หยุดสร้าง / copy pattern records เวลาดูด Duplicate product
	•	ถ้ายังมี table pattern อยู่ใน DB:
	•	ปล่อยไว้เป็น legacy data / reserved for future
	•	แต่อย่าผูก constraint ที่ทำให้ลบ product ไม่ได้เพราะ pattern
	•	ถ้า delete product แล้วมี pattern ผูกอยู่ → ลบ pattern ไปพร้อมกัน (soft delete ก็ได้)
แต่ทั้งหมดนี้ควรเป็น “invisible” สำหรับ user
	3.	Modal ใหม่ = Assets Only
	•	ด้านบน: Title เช่น Product Images — Tote
	•	มีแค่:
	•	Upload image
	•	แสดงรูป (grid) + ปุ่มลบเล็กๆ
	•	ไม่ต้องมี panel ขวา, ไม่ต้องมี pattern table
	4.	เผื่ออนาคต
	•	ถ้าวันหนึ่งจะทำ “Engineering / Pattern Module” จริง:
	•	แยกเป็นเมนู / modal ของตัวเอง
ไม่ผูกกับ Product แบบแนบไฟล์ดิบๆ แบบตอนนี้

⸻

ถ้าคุณโอเคกับทิศทางนี้ เดี๋ยวผมเขียน task26.9.md แบบเต็มๆ ให้เลยว่า
	•	ต้องลบ / ซ่อนอะไรใน view + JS
	•	ต้อง patch backend ตรงไหนไม่ให้ duplicate pattern ตาม product
	•	guardrail ว่าต่อไปห้ามกลับมาพ่วง pattern ที่ product อีก จนกว่าจะมี “Engineering/Pattern Module” จริงๆ

แล้วคุณก็เอาไปให้ Cursor รันต่อได้ทันทีครับ