ด้านล่างนี้คือ Guideline รุ่นสรุปแบบละเอียดที่สุด ที่ใช้เป็นแม่แบบสำหรับ Task 19.0 – 19.6 และเพื่อให้ Cursor / AI Agent ไม่ทำผิดแม้แต่นิดเดียว
(เพราะไฟล์ใน Cursor ที่คุณเปิดยังว่างอยู่ จึงสรุปให้ใหม่ครบทั้งหมด)

⸻

⭐ BELLAVIER SUPERDAG – GUIDELINE (FULL VERSION)

สำหรับ Task 19.0 – Task 19.6 (QC Safety, Condition Engine, UX Cleanup, Refactor)

เอกสารนี้คือ Single Source of Truth เพื่อควบคุมทิศทางของ SuperDAG จนจบ Phase 19
ทุกคำ ทุกหัวข้อ ถูกออกแบบภายใต้แนวคิด Luxury Engineering แบบ Hermès × Apple × Toyota TPS

⸻

🔶 I. เป้าหมายหลักของ Phase 19

1. Routing ต้อง “Deterministic” 100%

ห้าม token หลงทาง ห้าม fallback เงียบ ๆ
ถ้าข้อใดไม่ match → ถือว่าผิด → error screen ทันที

2. QC Logic ต้อง “เป็นระบบเดียว” และ predictable

ไม่พึ่งพา string match
ไม่พึ่ง fallback
ไม่ใช้ 2 ชุด evaluator

3. Condition System ต้อง unified

Decision Node legacy → deprecated
Conditional Edge → ตัวหลัก
Evaluator → ตัวเดียวทั้งระบบ

4. UX ต้อง simple แบบ iOS

ซ่อน complexity
ผู้ใช้ไม่ต้องกรอกสิ่งที่กระทบ logic

5. Engine ต้องพร้อมสำหรับ AI Optimization (Phase 20+)

ข้อมูล routing, qc_result, defect log ต้อง standardized

⸻

🔶 II. QC Integration Guideline

✔ 1. ความจริงหนึ่งเดียว (QC Result → Metadata + Event Log)

ทุกครั้งที่ QC เสร็จ:

token.metadata.qc_result

ใช้สำหรับ Routing
ตัวอย่าง:

{
  "status": "fail_major",
  "defect_type": "edge_crack",
  "severity": "major",
  "timestamp": "2025-12-19T14:32",
  "operator_id": 1023
}

flow_token_event

ใช้สำหรับ Traceability
เก็บแบบ immutable

หมายเหตุ
QC Technician ไม่สามารถพิมพ์ status เองได้
ป้องกัน error จาก free-text

⸻

🔶 III. Unified Condition Engine

ปัญหาเดิมที่ต้องแก้:
	•	decision node ใช้ logic A
	•	conditional edge ใช้ logic B
	•	QC ใช้ logic C (special-case)
	•	แล้ว engine พยายาม route ผ่านชุดฟังก์ชันที่ต่างกัน

→ เสี่ยงพัง
→ งง
→ test ยาก
→ debug ยาก
→ ไม่เหมาะกับ luxury workflow

เป้าหมายใหม่:
	•	evaluator เดียว
	•	API เดียว
	•	rule format เดียว
	•	ใช้กับ:
	•	QC → Edge
	•	Conditional edge
	•	Decision node (legacy)
	•	Machine rules (อนาคต)
	•	Expressive conditions (phase 21)

Condition Model ตัวเดียว:

{
  "type": "token_property",
  "property": "qc_result.status",
  "operator": "==",
  "value": "pass"
}

หรือ:

{
  "type": "job_property",
  "property": "priority",
  "operator": "in",
  "value": ["high","urgent"]
}

หรือใช้ expression:

"expression": "qc_result.severity == 'major' && token.qty >= 1"


⸻

🔶 IV. Deprecate Decision Node

เหตุผล:
	•	ผู้ใช้สับสน (มีสองระบบเงื่อนไข)
	•	ดูไม่เป็น iOS workflow
	•	ไม่เหมาะกับ luxury workflow design
	•	Duplication ของ logic
	•	Graph ไม่ clean
	•	Debug ยาก
	•	Routing เสี่ยงเกิด Side-effect

สิ่งที่ต้องทำ:
	•	disable การสร้าง decision node ใหม่
	•	migrate decision rule → conditional edge อัตโนมัติ
	•	mark decision node เป็น legacy (ยังอ่านได้ แต่แก้ไม่ได้)
	•	UI ซ่อน icon
	•	seed behavior ไม่ต้องรองรับอีกต่อไป

⸻

🔶 V. QC Routing Safety Model

❌ ห้าม fallback route

❌ ห้าม “first-edge wins”

✔ ต้องมีเส้นทางครบทุก status

✔ ต้อง validate ก่อน Save

✔ Graph ต้อง “ปิดเงื่อนไข” (complete conditional coverage)

ตัวอย่าง:

QC Status มี 3 ค่า:
	•	pass
	•	fail_minor
	•	fail_major

คุณต้องกำหนดเส้นทางแบบ explicit:
	•	pass → Finish
	•	fail_minor → Rework
	•	fail_major → Scrap หรือ QC_Exception_Sink

ห้ามปล่อยให้ค่าใด “ไม่มี route”

ถ้า user ลืม → UI แจ้ง error ตอน Save graph:

“QC condition incomplete: missing route for status = fail_major”

⸻

🔶 VI. UX Guideline – Luxury Grade

1. สิ่งที่ “ห้ามให้ผู้ใช้กรอกเอง”
	•	QC Status (ต้องเป็น dropdown)
	•	Defect Type (ต้องมาจาก master)
	•	Condition Key (เลือกจากรายการเท่านั้น)
	•	Condition Value (เลือกค่าที่ระบบเตรียมให้)
	•	JSON (ต้องซ่อนอยู่ภายใต้ “Advanced Mode” อย่างเดียว)

2. Conditional Edge UI ต้องเรียบง่าย

จาก:
	•	Condition Field
	•	Operator
	•	Value (free text)

เป็น:
	•	When “QC Status is” [dropdown]
	•	When “Defect Type is” [dropdown]
	•	When “Job Priority is” [dropdown]
	•	When “Quantity ≥” [input number]

โดยไม่มีช่องให้พิมพ์ string เทียบเอง

3. Machine / Split / Merge → Advanced mode
	•	ผู้ใช้ทั่วไปไม่ต้องเห็น concurrency, merge policy
	•	ซ่อนหลังปุ่ม “Advanced”

4. ซ่อน JSON ทั้งหมด
	•	form_schema_json
	•	qc_policy
	•	edge_condition (raw)

ต้อง generate อัตโนมัติ

⸻

🔶 VII. Graph Validation Rules (ก่อนเซฟ)
	1.	QC Node ต้องครอบคลุมทุก status
	2.	Conditional Edge ต้องไม่ overlap condition
	3.	Merge node ต้องมี incoming ≥2
	4.	Parallel split node ต้องมี outgoing ≥2
	5.	Node ไม่สามารถเป็น parallel split + merge พร้อมกัน
	6.	ห้ามมี Decision node ใหม่
	7.	Zero-matching-condition = error
	8.	Default route = explicit only, ไม่ใช่ fallback

⸻

🔶 VIII. Routing Engine Refactor Guideline

1. ลบ first-edge fallback

แทนด้วย error:

“RoutingError: no matching condition for QC result”

2. ลบ logic พิเศษของ QC (“หา qc=pass จาก string”)

ต้องเปลี่ยนเป็น unified evaluator:

ConditionEvaluator.evaluate(edgeCondition, context)

3. ทำ evaluator stateless & pure

เพื่อให้ test ได้ง่าย, deterministic, AI optimization ใช้ได้

4. QC fail logic = deterministic

ใช้:
	•	qc_policy
	•	qc_result.status
	•	conditional edge

⸻

🔶 IX. Debug & Error Handling Guideline

ถ้า routing ติด:
	•	token.state = “error_unroutable”
	•	log → service log
	•	event log → QC_ERROR
	•	UI แสดงตรงจุด:

“No route matches QC result: status=fail_major
Please define routing for this case.”

⸻

🔶 X. Summary – ในรูปแบบที่ Cursor/Agent ใช้งานได้ทันที

SuperDAG ต้องเป็นแบบ:
	•	เงื่อนไข = engine เดียว
	•	QC = standardized + deterministic
	•	Edge condition = UI ที่เรียบง่าย
	•	Decision node = legacy ห้ามสร้างใหม่
	•	No fallback route
	•	Validation ก่อน save
	•	JSON ซ่อนหมด
	•	Routing เดาทางไม่ได้ → ต้อง explicit เสมอ
	•	ทุก status ต้องมีเส้นทาง
	•	ผู้ใช้ไม่ต้องกรอกอะไรที่ส่งผลต่อ logic

นี่คือแนวคิด luxury-level ERP (Hermès grade) ที่คุณวางไว้

⸻