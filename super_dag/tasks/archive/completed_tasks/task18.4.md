# Task 18.4 — SuperDAG MAP (Read-Only Logic Discovery)

**Status:** NEW  
**Category:** SuperDAG – Static Analysis & System Mapping  
**Mode:** **READ-ONLY ANALYSIS** (no code changes in this task)

> เป้าหมาย: ให้ AI Agent ศึกษาและทำแผนที่ (MAP) Logic จริงของ SuperDAG ในระบบปัจจุบันแบบละเอียดที่สุด ก่อนจะมีการออกแบบ/รีแฟคเตอร์ใด ๆ เพิ่มเติม
> 
> **ห้ามเดา, ห้ามสมมติ, ห้ามเปลี่ยนโค้ด** – ดึงข้อมูลเฉพาะจากโค้ดที่มีอยู่เท่านั้น และสรุปออกมาเป็นเอกสาร

---

## 🎯 Objectives

1. ทำ “SuperDAG Map” ของ Bellavier Group ERP (คล้าย ERP_MAP แต่โฟกัสที่ DAG Engine)  
2. เข้าใจ Execution Model จริง ณ ปัจจุบัน ว่า token เดินอย่างไร, split/merge ทำงานอย่างไร, conditional edge ทำงานที่ไหน  
3. แยกแยะให้ชัดระหว่าง **สิ่งที่โค้ดทำจริง** กับ **สิ่งที่สเปกเคยตั้งใจจะทำ**  
4. หา gap / missing semantics / legacy logic ที่ยังหลงเหลืออยู่  
5. สร้างเอกสาร 4 ฉบับสำหรับมนุษย์ (ใช้ต่อใน task ถัดไป เช่น UX refactor, conditional edge redesign ฯลฯ)

**สำคัญ:**
- Task นี้ = "วิเคราะห์และเขียนเอกสาร" เท่านั้น  
- **ห้ามแก้ไขโค้ด, ห้ามสร้าง migration ใหม่, ห้ามแก้ UI** ใน task นี้  
- งาน refactor อื่น ๆ (เช่น Conditional Edge × QC, Start/Finish rules, QC UX) จะทำใน task ถัดไป โดยใช้เอกสารจาก Task 18.4 เป็นฐาน

---

## 📁 Files to Analyze (Source of Truth)

### Core Execution
- `source/BGERP/Service/DAGRoutingService.php`
- `source/BGERP/Service/TokenLifecycleService.php`
- `source/BGERP/Dag/ParallelMachineCoordinator.php`
- `source/BGERP/Dag/BehaviorExecutionService.php`
- `source/BGERP/Dag/DagExecutionService.php`

### Meta / Model
- `source/BGERP/SuperDAG/NodeTypeRegistry.php`
- `source/BGERP/SuperDAG/BehaviorRegistry.php` *(ถ้ามีอยู่จริง; ถ้าไม่มีให้บันทึกว่าไม่มี)*

### API & UI Integration
- `source/dag_routing_api.php`
- `assets/javascripts/dag/graph_designer.js`
- `assets/javascripts/dag/modules/GraphSaver.js`

### DB Schema (เฉพาะที่เกี่ยวกับ DAG)
- `database/tenant_migrations/**`  
  - ให้ค้นหาเฉพาะไฟล์ที่มี table: `routing_node`, `routing_edge`, `flow_token`  
  - ถ้ามี field อื่นที่ใช้ใน DAG runtime (เช่น merge policy, parallel flags, qc_policy ฯลฯ) ให้บันทึกด้วย

> ถ้าเจอไฟล์อื่นที่เกี่ยวข้องโดยตรงกับการรัน token หรือกำหนด behavior ของ graph ให้ระบุไว้ใน Map ด้วย

---

## 🧱 TASKS

### 1. High-level Architecture Map

**Output:** ในเอกสาร `SuperDAG_Architecture.md`

สำหรับแต่ละ Layer ให้ระบุ:

1. **Layer → File → Class → Method → Responsibility**  
   - Layer แนะนำ:
     - API Layer (REST / classic_api / dag_routing_api)  
     - Service Layer (DAGRoutingService, TokenLifecycleService ฯลฯ)  
     - DAG Engine Layer (DagExecutionService, BehaviorExecutionService, ParallelMachineCoordinator ฯลฯ)  
     - UI Integration (graph_designer.js, GraphSaver.js)  
     - DB / Persistence (migrations และ schema ของ routing_node / routing_edge / flow_token)

2. **Interaction Map**  
   - อธิบายว่า method ไหนเรียก method ไหน  
   - ระบุเป็น flow สั้น ๆ เช่น:  
     `dag_routing_api::saveGraph() → DAGRoutingService::saveGraphDefinition() → ... → DB`  
   - ถ้ามี diagram ให้ใช้ ASCII หรือ PlantUML ก็ได้

> ต้องอิงจากโค้ดจริงเท่านั้น — ห้ามเพิ่ม method/flow ที่ไม่มีในโค้ด

---

### 2. Full Execution Model (Token Flow)

**Output:** `SuperDAG_Execution_Model.md`

ตอบคำถามต่อไปนี้โดยอิงจากโค้ดจริง:

1. Token ถูกสร้างขึ้นที่ไหน? (`createToken`, `spawnToken`, ฯลฯ)
2. ฟังก์ชันใดเป็นจุด entry สำหรับการเดิน token? (เช่น `routeToken`, `advanceToken`)
3. เมื่อ node หนึ่ง "จบงาน" แล้ว อะไรเป็นตัวตัดสินว่า token จะไป node ถัดไป?
4. มีสถานะของ token อะไรบ้าง (เช่น READY, IN_PROGRESS, WAITING, COMPLETED, SCRAPPED ฯลฯ)?  
5. การหยุด/พัก/ยกเลิก token ทำงานอย่างไร? (ถ้ามี)

ควรมี diagram แสดงตัวอย่าง flow:
- Linear flow: Start → Operation → QC → Finish  
- Parallel flow: Operation → [Branch A, Branch B] → Merge → Finish  
- Flow ที่มี rework: QC → Rework Sink → กลับเข้าบาง node หรือจบ

> ถ้าคุณเจอ logic ที่ยังเป็น legacy หรือไม่ได้ใช้แล้ว ให้ระบุไว้ในส่วน "Legacy / Obsolete" ของไฟล์นี้

---

### 3. Join Semantics (Merge Behavior)

**Output:** section ให้รวมใน `SuperDAG_Execution_Model.md` หรือไฟล์ย่อยใน `SuperDAG_Flow_Map.md`

สำหรับการ join ที่มีการรอหลาย branch (merge node):

1. AND-Join (ต้องรอทุก branch หรือไม่?)  
2. ANY-Join (แค่สาขาใดสาขาหนึ่งถึง ก็ไปต่อได้หรือไม่?)  
3. AT_LEAST-Join (รอ N จาก M branch)  
4. TIMEOUT-Join (ถ้ารอนานเกิน timeout แล้วทำอย่างไร?)

ให้ตอบตามโค้ดจริงว่า:
- มี mode เหล่านี้หรือไม่?  
- ถ้ามีอยู่ ถูก implement ไว้ที่ไหน (method/class ไหน, field ไหนใน DB)?  
- ใช้ชื่อ field/enum อะไรจริง ๆ ในระบบ?

และอธิบายด้วยว่ามี OR-Join หรือ XOR-Join จริง ๆ หรือไม่ (ห้ามสมมติ)

---

### 4. Parallel Semantics (Parallel Tokens)

**Output:** รวมใน `SuperDAG_Flow_Map.md`

วิเคราะห์จาก `ParallelMachineCoordinator`, `DAGRoutingService`, `TokenLifecycleService` และ schema:

1. `parallel_group_id` ถูกสร้างขึ้นที่ไหน? (method/line จริง)  
2. `parallel_branch_key` ใช้แทนอะไร? ระบุที่มาของค่าจริง (เช่น index branch, edge id ฯลฯ)  
3. ตอนไหนที่ระบบตัดสินว่า "parallel block นี้พร้อม merge แล้ว"?  
   - ดูจากตาราง `flow_token` หรือ state อื่น ๆ อย่างไร?  
4. มี deadlock detection หรือไม่? ถูก implement ที่ไหน? ทำงานยังไงจริง ๆ?

ให้วาดตัวอย่าง parallel block 1 ชุด พร้อมอธิบาย step-by-step ว่า token แต่ละตัวเปลี่ยน state อย่างไรจนถึง merge

---

### 5. Conditional Routing Logic (สำคัญมาก)

**Output:** section เด่นใน `SuperDAG_Flow_Map.md`

> **เหตุผล:** ตอนนี้ทีมเริ่มจะ refactor Conditional Edge × QC, แต่เรายังไม่รู้ logic ปัจจุบันจริง ๆ ว่า evaluate edge ที่ไหน, อ่านค่าอะไรบ้าง

ให้ตอบคำถามต่อไปนี้แบบชัดเจน (พร้อมอ้างอิง method / field จริงในโค้ด):

1. **Edge conditions ถูก evaluate ที่ไหน?**  
   - ใน service/class ใด? method ไหน?  
   - ใช้ข้อมูลจาก token / form / qc_result จาก field ใดบ้าง?

2. **Model ของ Edge Condition ใน DB คืออะไร?**  
   - ตาราง/field ไหนเก็บ condition (เช่น `routing_edge.condition_field`, `condition_operator`, `condition_value` ฯลฯ)  
   - type ของค่าที่เก็บ (string, JSON ฯลฯ)

3. **Default routing ทำงานอย่างไร?**  
   - ถ้าไม่มีเงื่อนไขไหน match → edge ไหนถูกเลือกเป็นเส้นทาง default?  
   - มี flag/field บอก default edge หรือใช้ลำดับ edges ใน DB/graph?

4. **การทำงานร่วมกับ QC เป็นอย่างไรจริง ๆ?**  
   - Conditional edge ไปผูกกับผล QC อย่างไร (หรือไม่ได้ผูกเลย)?  
   - ใช้ field ใดจาก qc_result / form_schema? หรือเป็นการเปรียบเทียบ string ล้วน ๆ ที่ user กรอกเอง?  
   - ถ้าไม่มีข้อมูล QC (เช่น QC ยังไม่กรอกผล) engine ทำอย่างไรกับ edge เงื่อนไข?

> ห้ามออกแบบ behavior ใหม่ในไฟล์นี้ — บันทึกเฉพาะสิ่งที่โค้ดทำจริงเท่านั้น เพื่อใช้เป็นฐานสำหรับ task ถัดไป

---

### 6. Node Type & Behavior Semantics

**Output:** section ใน `SuperDAG_Architecture.md`

วิเคราะห์จาก `NodeTypeRegistry`, `BehaviorExecutionService`, `dag_routing_api`:

1. NodeType ถูก derive อย่างไร?  
   - ระบุ rule จริงที่ใช้ เช่น จาก `behavior_code`, `execution_mode`, flags บน node ฯลฯ  
2. `execution_mode` มีค่าประเภทใดบ้าง (จริงในโค้ด) เช่น `BATCH`, `HAT_SINGLE`, `QC_SINGLE` ฯลฯ  
3. Behavior registry (ถ้ามี) mapping อะไรบ้าง?  
4. Node ประเภท START, FINISH, OPERATION, QC, REWORK_SINK, ฯลฯ ถูกสร้างและถูกใช้ต่างกันอย่างไรใน engine

---

### 7. Machine Semantics

**Output:** รวมใน `SuperDAG_Execution_Model.md` หรือไฟล์ย่อยแยก `SuperDAG_Machine_Model.md`

วิเคราะห์จาก `ParallelMachineCoordinator`, service อื่น ๆ และ schema ที่เกี่ยวข้อง:

1. Machine binding ทำงานอย่างไร?  
   - token ถูกจับคู่กับ machine ตอนไหน?  
   - มี queue ต่อ machine หรือไม่ (ถ้ามี อยู่ที่ไหนในโค้ด)?

2. Concurrency limit ของ node/machine ถูกใช้ตรงไหน? (เช่น `concurrency_limit`, `max_sessions`)

3. `getETA` ทำงานอย่างไรจริง ๆ?  
   - ใช้สูตรอะไร, ใช้ข้อมูลใดบ้าง (WIP ปัจจุบัน, cycle time, ฯลฯ)

4. Deadlock detection (ถ้ามี) อยู่ตรงไหน และ logic คืออะไร?

---

### 8. Missing / Unimplemented Semantics

**Output:** `SuperDAG_Missing_Semantics.md`

จากการอ่านโค้ดทั้งหมด ให้ระบุรายการต่อไปนี้:

1. OR-Join มี implement จริงไหม? ถ้าไม่ มันอยู่ในรูปแบบ “TODO/Comment” หรือยังไม่ถูกแตะเลย?  
2. XOR-Join มีไหม? หรือ conditional edge ปัจจุบันทำหน้าที่นี้แบบหยาบ ๆ?  
3. Multi-source inbound / Error convergence / Re-entry (loopback) ทำงานอย่างไรใน engine ตอนนี้? มี code path รองรับไหม?  
4. Resource consolidation joins (เช่น รวมหลาย resource กลับมาใช้เครื่องเดียว) มีหรือไม่?  
5. ฟีเจอร์ที่เคยพูดถึงใน spec แต่ยังไม่เห็นในโค้ด (ให้ยกตัวอย่างและอ้างอิง spec ถ้าจำเป็น)

> จุดประสงค์ของไฟล์นี้คือให้เห็นภาพว่า "อะไรยังไม่มี" โดยเทียบกับแนวคิดของ workflow/DAG ทั่วไป แต่ยังคงยึดตามโค้ดเป็นหลัก ไม่ใช่จากความต้องการฝั่ง business

---

## 📄 Required Outputs (Files)

Agent ต้องสร้างไฟล์เอกสารอย่างน้อยดังนี้ (ในโฟลเดอร์ docs/super_dag/ หรือที่เหมาะสม):

1. **`docs/super_dag/SuperDAG_Architecture.md`**  
   - รายละเอียด Layer/File/Class/Method + Interaction Map + NodeType & Behavior semantics

2. **`docs/super_dag/SuperDAG_Flow_Map.md`**  
   - แผนที่การไหลของ token, parallel และ conditional routing  
   - มี diagram ASCII/PlantUML ถ้าเป็นไปได้

3. **`docs/super_dag/SuperDAG_Execution_Model.md`**  
   - อธิบาย execution step-by-step, state machine ของ token, machine semantics ฯลฯ

4. **`docs/super_dag/SuperDAG_Missing_Semantics.md`**  
   - สรุปสิ่งที่ engine ยังไม่รองรับหรือรองรับไม่ครบ เมื่อเทียบกับแนวคิด DAG ที่คาดไว้

> ถ้า Agent ต้องสร้างไฟล์ย่อยอื่น ๆ เพิ่มเพื่อให้เนื้อหาชัดเจนขึ้น สามารถทำได้ แต่ 4 ไฟล์ข้างต้นต้องมีแน่นอน

---

## 🚫 What This Task MUST NOT Do

เพื่อกัน AI Agent ใสซื่อและเผลอ refactor ให้ย้ำกติกาเหล่านี้อย่างชัดเจน:

1. **ห้ามแก้ code**  
   - ห้ามแก้ PHP, JS, migration, schema ใด ๆ ใน Task 18.4  
   - ถ้าพบ bug หรือโค้ดดูขัดแย้ง ให้แค่บันทึกลงในเอกสาร (เช่น ใน Missing_Semantics หรือใน section ปัญหา)

2. **ห้ามเขียน logic ใหม่**  
   - ห้ามเพิ่มฟังก์ชัน, ห้ามแก้ if/else, ห้ามเพิ่ม field ใหม่ใน DB  
   - ถ้าต้องการเสนอ improvement ให้เขียนในส่วน "Suggestion" ในเอกสารเท่านั้น

3. **ห้ามเดา behavior**  
   - ถ้าโค้ดไม่ชัดเจน ให้เขียนว่า "Unknown" หรือระบุว่า "ยังไม่พบจุดที่ใช้"  
   - ห้ามเขียนว่า "ระบบน่าจะทำ..." หรือ "ควรจะเป็นแบบ..." ในส่วนที่อธิบาย behavior ปัจจุบัน (สามารถเขียนในส่วนข้อเสนอได้)

4. **ห้ามผูกกับสเปกอนาคต**  
   - Task นี้ไม่ได้ออกแบบ SuperDAG รุ่นถัดไป  
   - เป้าหมายคือถอดแบบ logic ปัจจุบันออกมาให้แม่นที่สุด เพื่อใช้เป็นฐานของ Task ต่อไป เช่น 18.5, 19, 20

---

## ✅ Success Criteria

Task 18.4 ถือว่าเสร็จสมบูรณ์เมื่อ:

1. มีไฟล์ `SuperDAG_Architecture.md`, `SuperDAG_Flow_Map.md`, `SuperDAG_Execution_Model.md`, `SuperDAG_Missing_Semantics.md` อยู่จริงในโค้ด  
2. เอกสารทุกไฟล์อ้างอิง method/class/file ตามโค้ดจริง (สามารถใช้ค้นหากลับใน codebase ได้)  
3. ในส่วน Conditional Routing, Parallel, Merge, Machine, QC มีคำตอบชัดเจนว่าปัจจุบัน engine ทำอะไรอยู่บ้าง  
4. ไม่มี diff ของ code (PHP/JS/SQL) ที่ถูกแก้จาก Task 18.4 – เฉพาะไฟล์เอกสารเท่านั้นที่เปลี่ยน  
5. มนุษย์ (เช่น Nuttaphon) สามารถอ่านไฟล์เหล่านี้แล้วเข้าใจว่า SuperDAG ตอนนี้ “ทำงานจริงยังไง” โดยไม่ต้องเปิด code

---

หลังจาก Task 18.4 เสร็จ ทีมจะใช้เอกสารเหล่านี้ในการออกแบบ Task ถัดไป เช่น:
- Conditional Edge × QC Integration (design ใหม่บนพื้นฐานของ behavior ปัจจุบัน)  
- Node UX simplification เพิ่มเติม  
- การรองรับ OR-Join, XOR-Join, advanced merge ฯลฯ