

# Task 14.1 — Deep System Scan (Pre‑Cleanup Safety Phase)

> **Purpose:** ตรวจระบบจริงแบบละเอียด ก่อนเริ่ม Task 14 Blueprint Cleanup เพื่อป้องกันความเสี่ยงที่ AI Agent จะ deprecate หรือลบตารางผิดตัว โดยเฉพาะกลุ่ม Routing, Stock Pipeline, BOM และ Material System
>
> **ผลลัพธ์สุดท้ายของ Task 14.1:** ต้องได้เอกสารแสดง “ภาพจริงทั้งหมดของระบบ” ที่สามารถใช้เป็นแหล่งข้อมูลหลักก่อนเริ่ม Phase 14.2 – Migration Rewrite

---

## 🎯 Objectives (สิ่งที่ต้องทำใน Task 14.1)

### 1. Source Code Deep Scan
ทำการสแกนไฟล์โค้ดทั้งหมดในระบบ เพื่อหา reference ไปยัง legacy tables:
- `uom`, `stock_item`, `stock_item_lot`, `stock_item_asset`
- `bom_line`
- `routing` (legacy)
- UI หรือ API ที่ยังเรียกใช้ legacy schema

**ต้องค้นหา:**
- SELECT, INSERT, UPDATE, DELETE ที่กระทบ legacy tables
- จุดที่ยังอ่าน field เก่าที่ schema V2 ไม่มีแล้ว
- จุดที่ใช้ชื่อ column แบบเดิม เช่น `qty_sqft`, `material_sku`, `routing_id`
- จุด fallback ที่ใช้งาน data model V1 แบบอัตโนมัติโดยที่เราไม่รู้ตัว

**เครื่องมือที่แนะนำ:**
- Cursor search
- ripgrep (rg) หากใช้ได้
- Grep ผ่านเครื่องมือค้นหาใน IDE

ผลลัพธ์บันทึกในไฟล์ **task14.1_scan_results.md**

---

## 2. Migration Deep Scan
สแกน migration ทั้งหมด:
- ทั้ง global migration และ tenant migration
- ตรวจทุกไฟล์ว่ามี table legacy ถูกสร้างอยู่หรือไม่

**สิ่งที่ต้องระบุ:**
- ต้นทางที่สร้าง legacy tables
- ว่ามี “duplicate” table หรือไม่ เช่น `uom` vs `unit_of_measure`
- routing V1 ปนกับ routing V2 ที่สร้างซ้ำซ้อน
- stock_item pipeline ที่ยังถูก generate โดย migration เก่า

**ผลลัพธ์:**
ต้องบันทึกใน: **task14.1_migration_inventory.md**

---

## 3. Live Database Schema Scan (Dev Tenant + All Active Tenants)
ตรวจ schema ของฐานข้อมูลจริง:
- dev tenant (`bgerp_t_maison_atelier`)
- default tenant (`bgerp_t_default`)
- อื่น ๆ หากมี

**ระบุ:**
- ตารางทั้งหมดในฐานข้อมูล
- Foreign keys
- AUTO_INCREMENT values
- ตารางที่ไม่ควรอยู่แล้ว แต่ยังอยู่ใน DB
- ตารางที่ชื่อเหมือน แต่ belong ต่าง generation

เก็บผลลัพธ์ลงไฟล์: **task14.1_db_schema_snapshot.md**

---

## 4. Routing System Verification (ใช้ routing_classification.md เป็นกฎเหล็ก)
ตรวจสอบ routing tables ทุกตัว:

### ต้องจัดเข้าหมวดหมู่ดังนี้เท่านั้น:
- **Routing V2 (ห้ามแตะ / ห้ามลบ / ห้าม mark legacy)**
- **Routing V1 (legacy ที่ต้อง deprecate หลังสแกน)**

**และต้องตรวจในโค้ดว่า:**
- UI routing ตัวเก่า ยังเรียก V1 หรือไม่
- API ไหนยังใช้ routing V1
- งานที่ใช้ V2 อยู่แล้ว

บันทึกผลในไฟล์: **task14.1_routing_verification.md**

---

## 5. Stock Pipeline Verification
เพราะระบบ stock ของ Bellavier มีความซับซ้อนสูงและมี 2 generation ผสมกัน

ต้องตรวจว่า API/UI ใดใช้:
- `stock_item*` (legacy)
- `material*` (V2)
- `stock_ledger` (V2 movement)
- GRN Leather ใช้อะไรบ้าง
- CUT Behavior ใช้อะไรบ้าง
- ว่ามีใครพึ่งพา table legacy อยู่หรือไม่

บันทึกในไฟล์: **task14.1_stock_pipeline.md**

---

## 6. BOM Pipeline Verification
ต้องตรวจให้แน่ใจว่า:
- `bom` + `bom_item` ถูกใช้งานสำหรับ DAG V2 หรือไม่
- `bom_line` ยังถูกอ้างอิงอยู่ในระบบไหนบ้าง
- ต้อง confirm ว่า CUT Behavior เรียกใช้ตารางที่ถูกต้อง

บันทึกผลในไฟล์: **task14.1_bom_verification.md**

---

## 7. UI Scan (สำคัญมาก)
ตรวจว่า UI หน้าไหนยังลิงก์ไป legacy API เช่น:
- หน้า UOM เดิม
- หน้า Material Category เดิม
- หน้า Stock Adjustment เดิม
- หน้า Routing UI รุ่นเก่า

บันทึกผลในไฟล์: **task14.1_ui_scan.md**

---

## 8. Final Summary — “System Risk Map ก่อนเริ่ม Task 14”
รวมผลสแกนทั้งหมดเป็นไฟล์:

### ➤ `task14.1_final_risk_map.md`

ต้องประกอบด้วย:
- ตาราง V1 ที่ยังถูกใช้งาน
- ตาราง V1 ที่สามารถ deprecate ได้ทันที
- ตาราง V2 ที่ต้องปกป้อง (ห้าม drop)
- API/UI ที่อ้างอิง legacy
- Risk ของการลบ migration
- Workload ของ Task 14.2

---

## Deliverables (เอกสารที่ต้องได้จาก Agent)
เมื่อ Task 14.1 จบ ต้องมีไฟล์ดังนี้:
- task14.1_scan_results.md
- task14.1_migration_inventory.md
- task14.1_db_schema_snapshot.md
- task14.1_routing_verification.md
- task14.1_stock_pipeline.md
- task14.1_bom_verification.md
- task14.1_ui_scan.md
- task14.1_final_risk_map.md

---

## Acceptance Criteria
- Agent แยก V1/V2 ได้ถูกต้องทุกตาราง (เทียบกับ routing_classification.md และ schema V2 ปัจจุบัน)
- Agent ระบุ risk จุดอันตรายได้ (เช่น DAG, stock pipeline, bom pipeline)
- Agent วิเคราะห์ถูกร้อยละ 90+ ของระบบ
- ไม่มีการ generate migration หรือลบอะไรใน Task 14.1
- Task 14.1 เป็น **read‑only analysis** เท่านั้น

---

## หมายเหตุสำคัญ
Task 14.1 คือ safety layer ก่อน Task 14.2
ถ้าไม่มี Task นี้ การเริ่ม Task 14.2 จะมีความเสี่ยงสูงมากที่จะ:
- ทำลาย DAG Routing
- ทำลาย Stock Pipeline
- ทำลาย GRN Leather
- ทำลาย CUT Behavior
- ทำให้ Tenants เริ่มต้นล้มเหลว