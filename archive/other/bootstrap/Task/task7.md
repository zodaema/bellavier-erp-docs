Task7.md – Bootstrap Hardening & Legacy Cleanup

Task: Task 7
Date: 2025-11-18
Description:
งานนี้คือการทำ Hardening ระบบหลังการ Migration ทั้งหมดเสร็จสมบูรณ์ใน Task1–6 เพื่อให้โครงสร้าง API ภายใต้ระบบ Multi-Tenant แบบใหม่ (PSR-4 + TenantApiBootstrap) สะอาด, สม่ำเสมอ, และไม่มี legacy pattern เหลืออยู่ รวมถึงทำให้ Smoke Test สามารถเชื่อถือได้ 100% โดยไม่มี false positives อีกต่อไป

⸻

🎯 เป้าหมาย
	1.	ลบ legacy pattern ที่ยังหลงเหลือในไฟล์ API ที่ migrate แล้ว
	2.	ทำให้การเข้าถึง database tenant มีรูปแบบเดียวทั่วทั้งระบบ:
	•	Primary: $db (DatabaseHelper)
	•	Secondary (สำหรับ raw MySQL/DDL): $db->getTenantDb()
	3.	ลบการสร้าง connection/database helper ซ้ำ (new DatabaseHelper() หรือ new mysqli) ใน API layer
	4.	แก้และอัปเดต Smoke Test ให้ตรวจจับ pattern อย่างถูกต้อง
	•	ให้รู้จัก pattern ที่อนุญาตแล้ว
	•	ตัด false positive ออกให้หมด
	5.	ทำให้ไฟล์ทั้งหมดภายใต้ Tenant API layer ใช้มาตรฐานเดียวกัน (Final Bootstrap Standard)

⸻

📦 Scope ของ Task

ต้องแก้ไขไฟล์ 3 กลุ่ม:

⸻

กลุ่ม A – ไฟล์ API ที่มี Warning จาก Smoke Test

1. dag_routing_api.php
	•	บริเวณบรรทัด ~1460, 1465
	•	Smoke Test รายงานว่า $mysqli->query() อาจไม่มาจาก $db->getTenantDb()
	•	ต้องตรวจสอบว่าที่มาของ $mysqli เป็นแบบไหน
	•	ถ้ามาจาก $db->getTenantDb() → ให้ปรับ test
	•	ถ้าไม่ได้มาจาก bootstrap → ต้อง refactor ให้ใช้ $db->getTenantDb() เท่านั้น

⸻

2. hatthasilpa_job_ticket.php
	•	บริเวณ ~1733 มีการใช้:

new DatabaseHelper(...)

	•	ห้ามสร้าง DatabaseHelper ใน API layer แล้ว
	•	ต้อง refactor:
	•	ใช้ $db หลักจาก bootstrap แทนทุกกรณี
	•	ถ้า helper/service ต้องการ DatabaseHelper → ส่ง $db เดิมเข้าไป

⸻

3. team_api.php

ตำแหน่งที่ smoke test แจ้ง:
	•	line 116
	•	line 125
	•	line 134
	•	line 143

ทั้งหมดมี pattern:

new DatabaseHelper($tenantDb)

ต้อง:
	•	ลบทั้งหมด
	•	แทนที่ด้วย $db จาก bootstrap
	•	ทุก helper/service ที่อยู่ในไฟล์นี้ต้องรับ $db แทน $tenantDb

⸻

กลุ่ม B – ปรับ Smoke Test

ไฟล์: tests/bootstrap/ApiBootstrapSmokeTest.php

Smoke Test ตอนนี้เตือน false positives เช่น:
	•	$mysqli->query() ที่ได้รับจาก $db->getTenantDb()
	•	การสร้าง DatabaseHelper แบบ factory ที่จริงอาจอนุญาตใน helper (แต่ไม่อนุญาตใน API layer)

สิ่งที่ต้องทำ:

1. อัปเดต rule:
	•	อนุญาตให้ใช้ $db->getTenantDb()->query()
	•	ห้ามใช้ new mysqli
	•	ห้ามใช้ $mysqli->query() ถ้า $mysqli ไม่ใช่ตัวเดียวกับ $db->getTenantDb()

2. ปรับ regex สำหรับตรวจจับ new DatabaseHelper
	•	ถ้าอยู่ใน API layer → เตือน (ไม่อนุญาต)
	•	ถ้าอยู่ใน helper/service layer → อนุญาต

3. ตรวจสอบว่าทุกไฟล์ API มีการเรียก:

[$org, $db] = TenantApiBootstrap::init();

4. ตรวจสอบการลบ legacy functions:
	•	❌ resolve_current_org()
	•	❌ tenant_db()
	•	❌ new mysqli()
	•	❌ json_error/json_success (ต้องผ่าน JsonResponse)
	•	❌ new DatabaseHelper() (API layer)

หลังแก้แล้วต้องไม่มี warning ใน Smoke Test เลย

⸻

กลุ่ม C – มาตรฐาน Bootstrap ใหม่ (ตรวจให้ครบทุกไฟล์)

งานนี้ให้ Agent audit ไฟล์ API ทุกตัวที่ migrate แล้ว (40 ไฟล์)
เพื่อตรวจสอบว่า:

1. รูปแบบมาตรฐานการเริ่มต้นเหมือนกันทุกไฟล์

ต้องเป็น:

session_start();
require_once __DIR__ . '/../vendor/autoload.php';
require_once __DIR__ . '/../config.php';

use BGERP\Bootstrap\TenantApiBootstrap;
[$org, $db] = TenantApiBootstrap::init();

2. ไม่มีการ set header ซ้ำซ้อน

เช่น:
	•	Content-Type
	•	Cache-Control

เพราะ JsonResponse จัดการให้แล้ว

3. การใช้ DB ไม่หลุด pattern

ถูกต้อง:
	•	$db->fetchAll()
	•	$db->fetchOne()
	•	$db->execute()
	•	$db->getTenantDb()->query()

ผิด:
	•	new DatabaseHelper()
	•	$conn = tenant_db()
	•	$mysqli = new mysqli(...)
	•	$tenantDb = tenant_db() แล้วใช้ตรง ๆ

4. ห้ามใช้ resolve_current_org() ใน business logic

ต้องใช้ $org จาก bootstrap เท่านั้น

⸻

🧱 Guardrails (ห้ามแตะ)
	1.	ห้ามเปลี่ยน business logic ในไฟล์ API ทั้งหมด
	2.	ห้ามแก้ SQL query เนื้อหา / condition
	3.	ห้ามแก้ JSON structure ที่ API ส่งกลับ
	4.	ห้ามแก้ invariants ของ:
	•	Token Engine
	•	Work Session
	•	Routing Graph
	•	Assignment Rules
	5.	ห้ามแก้ signature ของ Helper/Service ที่ถูกใช้ใน unit test หรือ integration test

⸻

🔧 ขั้นตอนการทำงาน (ให้ Agent ทำตาม)

Step 1 – Fix dag_routing_api.php
	•	ตรวจสอบที่มาของ $mysqli
	•	ถ้าถูกต้องแล้ว → comment + adjust smoke test
	•	ถ้าผิด → refactor ให้ใช้ $db->getTenantDb()

Step 2 – Fix hatthasilpa_job_ticket.php
	•	ลบ new DatabaseHelper
	•	แทนด้วย $db ที่ bootstrap ให้มา

Step 3 – Fix team_api.php
	•	ลบ new DatabaseHelper ทุกรายการ
	•	ส่ง $db ให้ helpers/services แทน

Step 4 – Update Smoke Test
	•	ปรับ rule ให้ตรงกับ “Final Bootstrap Standard”

Step 5 – Run Verification

Agent ต้องรัน:

php -l source/dag_routing_api.php
php -l source/hatthasilpa_job_ticket.php
php -l source/team_api.php
php tests/bootstrap/ApiBootstrapSmokeTest.php
vendor/bin/phpunit tests/Unit
vendor/bin/phpunit tests/Integration

ทั้งหมดต้องผ่านโดยไม่มี warning

⸻

📌 Output ที่ต้องส่งกลับ

เมื่อ Task 7 เสร็จ Agent ต้องส่งรายการนี้กลับ:
	1.	รายงานไฟล์ที่ปรับปรุง พร้อมจำนวนจุดที่แก้ไข
	2.	รายงาน false positive ที่ถูกลบ + test ที่ถูกอัปเดต
	3.	คอนเฟิร์มว่า:
	•	ไม่มี legacy pattern ใด ๆ เหลือใน Tenant APIs
	•	Bootstrap standard ใช้แบบเดียวกันทุกไฟล์
	4.	Screenshot (หรือ log) ของ Smoke Test & PHPUnit ที่ผ่านทั้งหมด
	5.	Summary ว่า ERPCore API Layer เข้าสู่ “BSO (Bootstrap Standard Operation) Mode”